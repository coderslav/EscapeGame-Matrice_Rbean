<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <title>EscapeBooking</title>

        <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-Vkoo8x4CGsO3+Hhxv8T/Q5PaXtkKtu6ug5TOeNV6gBiFeWPGFN9MuhOf23Q9Ifjh" crossorigin="anonymous">
    </head>
    <body>
        <div class="container">

            
            <nav class="navbar navbar-expand-lg navbar-light bg-light d-flex justify-content-between">
                <a class="navbar-brand" href="/">Home</a>
                <div>
                    {{#if user}}
                        <a class="nav-item btn btn-info" href="/bookings">Voir mes réservations</a>
                        <a class="nav-item btn btn-dark" href="/logout">Déconnexion</a>
                    {{else}}
                        <a class="nav-item btn btn-dark" href="/logout">Connexion</a>
                    {{/if}}
                </div>
            </nav>

            {{#if message}}
                <div class="alert {{messageClass}} mt-4" role="alert">
                    {{message}}
                </div>
            {{/if}}

            {{{body}}}

        </div>
    </body>
</html>